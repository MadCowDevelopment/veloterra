# 4.1 Business Context

<details>
<summary>Documentation Hints</summary>

Show the system boundary, users, neighboring systems, and exchanged information.
</details>

```mermaid
graph LR
    Cyclist[Cyclist] -->|starts and records rides| VeloTerra[VeloTerra PWA]
    VeloTerra -->|map requests| OFM[OpenFreeMap]
    VeloTerra -->|optional sign-in| Identity[Google and Microsoft OAuth]
    VeloTerra <-->|optional private progress sync| Supabase[Supabase]
    GitHub[GitHub Pages] -->|serves app and updates| VeloTerra
    Device[Browser and device APIs] <-->|GPS, wake lock, storage, cache| VeloTerra
```

| Neighbor | Information Exchanged |
|---|---|
| Cyclist | Ride commands, settings, map interaction; ride status, progress, history, and wallet. |
| Browser/device APIs | Position fixes, wake-lock lifecycle, IndexedDB records, local preferences, service-worker caches. |
| OpenFreeMap | Style JSON, vector/raster tiles, glyphs, and sprites. |
| Google and Microsoft OAuth | User authentication through Supabase-managed OAuth flow; Microsoft sign-in is backed by Microsoft Entra ID. |
| Supabase | Auth session plus private explored-cell, wallet, and ride records. |
| GitHub Pages | Versioned static application assets and PWA updates. |